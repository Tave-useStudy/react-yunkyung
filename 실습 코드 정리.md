# 1주차 실습

1주차 개념이 실습 코드에 어떻게 적용되었는지 정리.

| 개념 | 쓰인 파일 |
|---|---|
| useState | `App.jsx`, `TextInput.jsx`, `DayTodos.jsx`, `TaskEditForm.jsx`, `TodoDetailPage.jsx` |
| useEffect | `App.jsx` |
| useRef | `TextInput.jsx` |
| React 19 Ref 전달 | `TextField.jsx` ← `TextInput.jsx` |
| 조건부 렌더링 | `DayTodos.jsx`, `TaskItem.jsx`, `TodoDetailPage.jsx`, `TextField.jsx`, `CalendarDay.jsx`, `TaskList.jsx` |

---

## 1. useState

### 개념

```jsx
const [state, setState] = useState(초기값);
```

- `state` — 현재 값
- `setState` — 값을 바꾸는 함수. 호출하면 컴포넌트가 **다시 렌더링**되어 새 값이 화면에 보인다
- `초기값` — 처음 렌더링할 때 한 번만 쓰인다

### 예시

**① 입력창 글자 — `src/components/TextInput.jsx`**

```jsx
const [text, setText] = useState('');
const [category, setCategory] = useState(settings.defaultCategory);

<TextField value={text} onChange={setText} ... />

<select value={category} onChange={(e) => setCategory(e.target.value)}>
```

글자를 입력할 때마다 `setText`가 값을 바꾸고, 화면이 다시 그려져 입력창과 글자 수(`3 / 20`)가 갱신된다. 할 일을 추가한 뒤 `setText('')`로 입력창을 비운다.

**② 할 일 목록 — `src/App.jsx`**

```jsx
const [todos, setTodos] = useState(loadTodos);

const addTodo = ({ text, category, date }) => {
  setTodos((prev) => [
    ...prev,
    { id: Date.now(), text, memo: '', done: false, category, date, createdAt: Date.now() },
  ]);
};

const toggleTodo = (id) => {
  setTodos((prev) =>
    prev.map((todo) => (todo.id === id ? { ...todo, done: !todo.done } : todo))
  );
};

const deleteTodo = (id) => {
  setTodos((prev) => prev.filter((todo) => todo.id !== id));
};
```

체크박스를 누르면 `setTodos`가 목록을 바꾸고, 화면이 다시 그려져 취소선이 생긴다.
- 배열은 직접 고치지 않고 `...` / `map` / `filter`로 **새 배열**을 만든다
- `setTodos(prev => ...)` — 항상 직전 값을 기준으로 계산한다
- `useState(loadTodos)` — 함수를 넘기면 처음 한 번만 실행되어 localStorage에서 불러온다

**③ 화면 모드 — `src/components/DayTodos.jsx`**

```jsx
const [isAdding, setIsAdding] = useState(false);   // 입력 폼이 열려 있는지
const [tab, setTab] = useState('all');             // 전체 / 미완료 / 완료

<button className="primary-btn" onClick={() => setIsAdding(true)}>
  할 일 입력하기
</button>
```

"할 일 입력하기"를 누르면 `isAdding`이 `true`가 되고, 화면이 다시 그려져 버튼 자리에 입력 폼이 나타난다.

**④ 편집 모드 — `src/pages/TodoDetailPage.jsx`**

```jsx
const [isEditing, setIsEditing] = useState(false);

<button onClick={() => setIsEditing(true)}>수정</button>
```

**⑤ 객체 state — `src/components/TaskEditForm.jsx`**

```jsx
const [draft, setDraft] = useState({
  text: todo.text,
  category: todo.category,
  date: todo.date,
  memo: todo.memo,
});

const updateDraft = (field, value) =>
  setDraft((prev) => ({ ...prev, [field]: value }));

<textarea value={draft.memo} onChange={(e) => updateDraft('memo', e.target.value)} />
```

객체도 직접 고치지 않고, 복사(`...prev`)한 뒤 바꿀 필드만 덮어쓴 **새 객체**를 넘긴다.

---

## 2. useEffect

### 개념

`useEffect`는 Component가 렌더링된 후에 실행할 작업을 정의하는 Hook.

```jsx
useEffect(() => {
  // 렌더링 후 실행할 작업
  return () => {
    // 정리 (사라질 때 / 다음 실행 전)
  };
}, [의존성]);
```

| 의존성 배열 | 실행 시점 |
|---|---|
| `[]` | 처음 화면에 나타날 때 한 번 |
| `[a]` | 처음 + `a`가 바뀔 때마다 |
| 없음 | 매 렌더링 후 |

### 예시

**할 일이 바뀔 때마다 저장 — `src/App.jsx`**

```jsx
const STORAGE_KEY = 'todos';

useEffect(() => {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(todos));
}, [todos]);
```

할 일을 추가·완료·삭제해서 `todos`가 바뀌면, 화면이 다시 그려진 **후에** localStorage에 저장한다. 그래서 새로고침해도 목록이 남아 있다.

- 의존성이 `[todos]`라 `todos`가 바뀔 때만 실행된다
- 정리할 것(타이머, 구독)이 없어서 `return`은 없다
- 프로젝트의 **유일한** effect. localStorage 같은 **외부 시스템과 맞추는 일**에만 effect를 쓰고, 버튼 클릭·저장 같은 사용자 행동은 이벤트 핸들러에서 처리했다

### 예시 (투두리스트에 없는 부분)

투두리스트에는 **빈 배열 `[]`** 과 **정리 함수 `return`** 을 쓰는 effect가 없어서 예시로 정리.

**타이머 켜고 끄기**

```jsx
import { useEffect } from 'react';

function Timer() {
  useEffect(() => {
    const id = setInterval(() => console.log('1초'), 1000);
    return () => clearInterval(id);
  }, []);

  return <p>콘솔을 확인하세요</p>;
}
```

화면이 처음 나타날 때 타이머를 켜고, 사라질 때 끈다. `[]`가 처음 한 번만, `return`이 끝날 때 정리.
`return`으로 정리하지 않으면 컴포넌트가 사라져도 타이머가 계속 돌아간다.

**처음 한 번 데이터 불러오기**

```jsx
import { useEffect, useState } from 'react';

function UserProfile() {
  const [user, setUser] = useState(null);

  useEffect(() => {
    let ignore = false;

    fetch('https://randomuser.me/api/')
      .then((res) => res.json())
      .then((data) => {
        if (!ignore) setUser(data.results[0]);
      });

    return () => {
      ignore = true;   // 사라진 뒤 도착한 응답은 무시
    };
  }, []);

  return <p>{user ? user.name.first : '불러오는 중...'}</p>;
}
```

화면이 처음 나타날 때 한 번 API를 호출한다. 개발 모드(StrictMode)에서는 effect가 두 번 실행되므로, `ignore` 플래그로 먼저 끝난 요청의 결과를 무시한다.

---

## 3. useRef

### 개념

`useRef`는 렌더링과 관계없이 값을 기억하거나 DOM 요소에 접근할 때 사용하는 Hook.

```jsx
const ref = useRef(초기값);
ref.current   // 저장된 값 / 연결된 DOM 요소
```

`ref.current`를 바꿔도 화면은 다시 그려지지 않는다.

### 예시

**할 일 추가 후 입력창에 커서 두기 — `src/components/TextInput.jsx`**

```jsx
const inputRef = useRef(null);

const handleSubmit = (e) => {
  e.preventDefault();
  if (isDisabled) return;

  onAdd({ text: trimmed, category });
  setText('');
  inputRef.current?.focus();   // 버튼 클릭으로 추가해도 다시 입력창에 포커스
};

<TextField ref={inputRef} ... />
```

"추가" 버튼을 누르면 커서가 버튼으로 옮겨 가는데, `inputRef.current.focus()`로 다시 입력창에 커서를 둔다. 덕분에 바로 다음 할 일을 이어서 적을 수 있다. 이 과정에서 화면은 다시 그려지지 않는다.

- `document.querySelector('input').focus()` 같은 DOM 직접 조작 대신 ref를 쓴다
- `?.` — 아직 연결 전(`null`)이어도 에러 없이 넘어간다

### 예시 (투두리스트에 없는 부분)

투두리스트에서는 DOM 접근에만 썼고, **렌더링과 관계없이 값을 기억**하는 용도는 없어서 예시로 정리.

**버튼을 몇 번 눌렀는지 기억하기 (화면에는 안 보이는 값)**

```jsx
import { useRef } from 'react';

function ClickCounter() {
  const countRef = useRef(0);

  const handleClick = () => {
    countRef.current += 1;              // 값은 바뀌지만 화면은 다시 그려지지 않는다
    console.log(`${countRef.current}번 클릭`);
  };

  return <button onClick={handleClick}>클릭</button>;
}
```

**타이머 id 기억하기 (시작 / 정지)**

```jsx
import { useRef, useState } from 'react';

function Stopwatch() {
  const [seconds, setSeconds] = useState(0);
  const timerRef = useRef(null);        // 타이머 id는 화면에 보일 필요가 없다

  const start = () => {
    if (timerRef.current) return;
    timerRef.current = setInterval(() => setSeconds((s) => s + 1), 1000);
  };

  const stop = () => {
    clearInterval(timerRef.current);
    timerRef.current = null;
  };

  return (
    <>
      <p>{seconds}초</p>
      <button onClick={start}>시작</button>
      <button onClick={stop}>정지</button>
    </>
  );
}
```

화면에 보여야 하는 `seconds`는 state, 화면과 상관없는 타이머 id는 ref.
id를 일반 변수(`let timerId`)에 두면 다시 렌더링될 때마다 초기화되어 정지할 수 없다.

---

## 4. React 19 Ref 전달

### 개념

컴포넌트에 ref를 다는 방법. `ref`를 props처럼 받아서 안쪽 `input`에 달아준다.

```jsx
function MyInput({ ref }) {
  return <input ref={ref} />;
}

<MyInput ref={inputRef} />
```

예전엔 `forwardRef`로 감싸야 했지만, 19부터는 ref가 그냥 일반 prop이라 받아서 연결하면 된다.

```jsx
// React 18까지
const MyInput = forwardRef(function MyInput(props, ref) {
  return <input ref={ref} />;
});
```

### 예시

**글자 수가 붙은 입력창 — `src/components/TextField.jsx`**

```jsx
// React 19: ref를 forwardRef 없이 일반 prop으로 받는다.
function TextField({ ref, maxLength, value, onChange, ...rest }) {
  const isOverLimit = value.length > maxLength;

  return (
    <div className="text-field">
      <input
        ref={ref}
        type="text"
        value={value}
        onChange={(e) => onChange(e.target.value)}
        {...rest}
      />
      <p className={isOverLimit ? 'count over' : 'count'}>
        {value.length} / {maxLength}
      </p>
      {isOverLimit ? (
        <p className="warning">최대 {maxLength}자까지 입력할 수 있습니다.</p>
      ) : null}
    </div>
  );
}
```

**ref를 넘기는 쪽 — `src/components/TextInput.jsx`**

```jsx
const inputRef = useRef(null);

<TextField
  ref={inputRef}
  value={text}
  onChange={setText}
  maxLength={MAX_TEXT_LENGTH}
  placeholder="할 일을 입력하세요"
/>
```

`TextInput`의 `inputRef`가 `TextField`를 거쳐 안쪽 `<input>`에 연결된다. 그래서 바깥(`TextInput`)에서 `inputRef.current.focus()`가 된다.

---

## 5. 조건부 렌더링

### 개념

데이터 값에 따라 다른 화면을 보여주는 것. 화면을 직접 숨기고 보이는 게 아니라, **어떤 데이터일 때 어떤 화면인지를 정의**한다.

```jsx
{조건 ? <A /> : <B />}   // 둘 중 하나
{조건 && <A />}           // 조건이 참일 때만
if (조건) return <A />;   // 화면 구조가 완전히 다를 때 (early return)
```

> `&&` 주의 — 조건이 숫자 `0`이면 화면에 `0`이 찍힌다. 숫자일 수 있으면 `조건 ? <A /> : null`로 쓴다.

### 예시

**① 둘 중 하나 — 버튼 ↔ 입력 폼 (`src/components/DayTodos.jsx`)**

```jsx
{isAdding ? (
  <TextInput onClose={() => setIsAdding(false)} ... />
) : (
  <button className="primary-btn" onClick={() => setIsAdding(true)}>
    할 일 입력하기
  </button>
)}
```

`isAdding` 값에 따라 입력 폼과 보라색 버튼 중 하나가 나온다.

**② 둘 중 하나 — 빈 안내 ↔ 목록 (`src/components/DayTodos.jsx`)**

```jsx
{dayTodos.length === 0 ? (
  <p className="empty">첫 할 일을 추가해 보세요.</p>
) : (
  <>
    <FilterTabs active={tab} counts={counts} onChange={setTab} />
    <TaskList todos={visibleTodos} ... />
  </>
)}
```

**③ 있을 때만 — 글자 수 초과 경고 (`src/components/TextField.jsx`)**

```jsx
{isOverLimit ? (
  <p className="warning">최대 {maxLength}자까지 입력할 수 있습니다.</p>
) : null}
```

20자를 넘었을 때만 경고 문구가 나온다.

**④ 있을 때만 — 달력 칸 개수 뱃지 (`src/components/CalendarDay.jsx`)**

```jsx
{count ? (
  <span className={allDone ? 'day-count all-done' : 'day-count'}>
    {allDone ? '✓' : count.all - count.done}
  </span>
) : null}
```

할 일이 있는 날만 뱃지가 나오고, 다 끝냈으면 ✓, 아니면 남은 개수를 보여준다.

**⑤ 클래스 바꾸기 — 완료 표시 (`src/components/TaskItem.jsx`)**

```jsx
<li className={todo.done ? 'task done' : 'task'}>
```

`done`이면 취소선 + 흐리게, 아니면 선명하게 보인다.

**⑥ early return — 상세 페이지 (`src/pages/TodoDetailPage.jsx`)**

```jsx
if (!todo) {
  return (
    <section className="detail">
      <p className="empty">해당 할 일을 찾을 수 없습니다. (id: {id})</p>
      <Link to="/">목록으로</Link>
    </section>
  );
}

if (isEditing) {
  return (
    <section className="detail">
      <h2>할 일 수정</h2>
      <TaskEditForm todo={todo} onSave={handleSave} onCancel={() => setIsEditing(false)} />
    </section>
  );
}

return <section className="detail">...상세 정보...</section>;
```

없는 할 일 → 안내, 편집 중 → 수정 폼, 그 외 → 상세 정보. 화면 구조가 완전히 달라서 JSX 안에서 나누지 않고 `return`으로 먼저 내보낸다.

**⑦ 세부사항 있음 ↔ 없음 (`src/pages/TodoDetailPage.jsx`)**

```jsx
{todo.memo ? (
  <p className="memo">{todo.memo}</p>
) : (
  <p className="memo empty">아직 세부사항이 없어요. 수정을 눌러 적어 보세요.</p>
)}
```

### 예시 (투두리스트에 없는 부분)

투두리스트에서는 모두 삼항으로 바꿔서 **`&&`** 를 쓰는 곳이 없어서 예시로 정리.

```jsx
function Status({ isLoggedIn, error }) {
  return (
    <>
      {isLoggedIn ? <p>환영합니다</p> : <button>로그인</button>}
      {error && <p>에러가 났습니다</p>}
    </>
  );
}
```

`isLoggedIn` 값에 따라 둘 중 하나가 나오고, `error`가 있을 때만 문구가 나온다.

**`&&`를 쓰면 안 되는 경우**

```jsx
const count = 0;

{count && <p>{count}개 남음</p>}           // 화면에 "0"이 찍힌다
{count > 0 && <p>{count}개 남음</p>}       // O: 비교식은 항상 true / false
{count ? <p>{count}개 남음</p> : null}     // O: 삼항
```

`&&` 왼쪽이 **true / false**면 안전하고, **숫자**일 수 있으면 비교식이나 삼항으로 쓴다.

---

## 정리

| 개념 | 한 줄 요약 | 프로젝트 코드 |
|---|---|---|
| useState | 바뀌면 화면이 다시 그려지는 값 | `const [isAdding, setIsAdding] = useState(false)` |
| useEffect | 렌더링 후 외부 시스템과 동기화 | `useEffect(() => localStorage.setItem(...), [todos])` |
| useRef | 리렌더링 없이 값 기억 / DOM 접근 | `inputRef.current?.focus()` |
| Ref 전달 (19) | `ref`도 그냥 prop | `function TextField({ ref, ... })` |
| 조건부 렌더링 | 데이터 → 어떤 화면인지 정의 | `isAdding ? <TextInput /> : <button>` |


---
---

# 2주차 실습

2주차 개념 React Router가 실습 코드에 어떻게 적용되었는지 정리.

| 개념 | 쓰인 파일 |
|---|---|
| SPA 라우팅 | 프로젝트 전체 (페이지 3개: 메인 / 상세 / 설정) |
| BrowserRouter | `main.jsx` |
| Routes / Route | `App.jsx` |
| Link / NavLink | `TaskItem.jsx`, `TodoDetailPage.jsx`, `Layout.jsx` |
| useNavigate | `TodoDetailPage.jsx`, `SettingsPage.jsx` |
| useParams | `TodoDetailPage.jsx` |
| 중첩 라우트 (Outlet) | `App.jsx` + `Layout.jsx` |

---

## 1. SPA에서 라우팅이란

### 개념

**SPA(Single Page Application)** 는 HTML 파일이 **하나**(`index.html`)뿐인 웹앱이다.
처음 한 번만 페이지를 받아 오고, 그 뒤로는 JavaScript가 화면만 바꿔 끼운다.

| | 전통적인 웹사이트 (MPA) | SPA |
|---|---|---|
| 페이지 이동 | 서버에 새 HTML 요청 → 전체 새로고침 | JS가 URL만 바꾸고 필요한 컴포넌트만 교체 |
| 화면 깜빡임 | 있음 | 없음 |
| state | 이동하면 사라짐 | 유지됨 |

**라우팅** = "지금 URL이 무엇이냐에 따라 **어떤 컴포넌트를 보여줄지** 정하는 것".
React에는 라우팅 기능이 없어서 **React Router**(`react-router-dom`)를 설치해서 쓴다.

```bash
npm install react-router-dom
```

### 예시

**To-Do-List의 페이지 구성**

| URL | 보여줄 컴포넌트 | 화면 |
|---|---|---|
| `/` | `MainPage` | 달력 + 선택한 날의 할 일 |
| `/todos/1727236800000` | `TodoDetailPage` | 할 일 하나의 상세 / 수정 / 세부사항 |
| `/settings` | `SettingsPage` | 기본 카테고리·정렬, 일괄 삭제 |
| 그 외 | `/`로 이동 | — |

```
index.html  (하나뿐)
 └─ <div id="root">
      └─ App
           ├─ URL이 /           → MainPage
           ├─ URL이 /todos/:id  → TodoDetailPage
           └─ URL이 /settings   → SettingsPage
```

페이지를 오가도 새로고침이 일어나지 않아서, `App`의 `todos` state가 그대로 유지된다.
상세 페이지에서 할 일을 수정하고 메인으로 돌아오면 바로 반영된 목록이 보이는 이유다.

---

## 2. BrowserRouter / Routes / Route / Link 기본 구조

### 개념

| 컴포넌트 | 역할 |
|---|---|
| `<BrowserRouter>` | 앱 전체를 감싸서 라우팅 기능을 켠다. 브라우저 주소창(URL)과 연결 |
| `<Routes>` | `<Route>`들을 모아 두고, 현재 URL과 **맞는 것 하나**를 골라 그린다 |
| `<Route path element>` | "이 주소(`path`)면 이 화면(`element`)" 규칙 하나 |
| `<Link to>` | 새로고침 없이 이동하는 링크. `<a href>` 대신 쓴다 |
| `<NavLink to>` | `Link` + 현재 주소와 같으면 자동으로 `class="active"` |

```jsx
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Home />} />
    <Route path="/about" element={<About />} />
  </Routes>
</BrowserRouter>

<Link to="/about">소개</Link>
```

> `<a href="/about">`을 쓰면 브라우저가 페이지를 **새로 요청**해서 state가 전부 사라진다. SPA에서는 항상 `<Link>`.

### 예시

**① BrowserRouter — `src/main.jsx`**

```jsx
import { BrowserRouter } from 'react-router-dom';

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
);
```

가장 바깥에서 `App`을 감싼다. 그래야 `App` 안의 모든 컴포넌트가 `Link`, `useNavigate`, `useParams`를 쓸 수 있다.

**② Routes / Route — `src/App.jsx`**

```jsx
<Routes>
  <Route element={<Layout />}>
    <Route
      index
      element={
        <MainPage
          todos={todos}
          settings={settings}
          onAdd={addTodo}
          onToggle={toggleTodo}
          onDelete={deleteTodo}
        />
      }
    />
    <Route
      path="todos/:id"
      element={
        <TodoDetailPage
          todos={todos}
          onToggle={toggleTodo}
          onDelete={deleteTodo}
          onUpdate={updateTodo}
        />
      }
    />
    <Route
      path="settings"
      element={
        <SettingsPage
          settings={settings}
          todos={todos}
          onChangeSettings={setSettings}
          onClearDone={clearDone}
          onClearAll={clearAll}
        />
      }
    />
    <Route path="*" element={<Navigate to="/" replace />} />
  </Route>
</Routes>
```

- `index` — 부모 경로(`/`) 그 자체일 때 보여줄 화면
- `path="todos/:id"` — `:id` 자리에 어떤 값이 와도 맞는다 (`/todos/1`, `/todos/abc` …)
- `path="*"` — 위의 어떤 규칙에도 안 맞는 주소. `<Navigate>`로 메인으로 보낸다
- `element`에 props를 넘길 수 있다 → 세 페이지가 `App`의 같은 `todos`를 본다

**③ Link — 할 일 제목을 누르면 상세 페이지로 (`src/components/TaskItem.jsx`)**

```jsx
import { Link } from 'react-router-dom';

<Link to={`/todos/${todo.id}`} className="task-text">
  {todo.text}
</Link>
```

**④ Link — 상세 페이지에서 그날 목록으로 (`src/pages/TodoDetailPage.jsx`)**

```jsx
<dt>날짜</dt>
<dd>
  <Link to={`/?date=${todo.date}`}>{todo.date}</Link>
</dd>
```

**⑤ NavLink — 헤더 메뉴 (`src/components/Layout.jsx`)**

```jsx
<nav className="app-nav">
  <NavLink to="/" end>
    달력
  </NavLink>
  <NavLink to="/settings">설정</NavLink>
</nav>
```

```css
/* App.css — 현재 페이지 메뉴만 보라색 */
.app-nav a.active {
  background: var(--accent-bg);
  color: var(--accent);
}
```

- `end` — `/`는 모든 주소의 시작이라 `end`가 없으면 `/settings`에서도 "달력"이 active가 된다. `end`를 붙이면 **정확히 `/`일 때만** active

---

## 3. useNavigate / useParams 사용법

### 개념

| Hook | 역할 | 사용법 |
|---|---|---|
| `useNavigate()` | **코드로** 페이지 이동 | `const navigate = useNavigate(); navigate('/경로')` |
| `useParams()` | URL의 `:이름` 자리 값 꺼내기 | `const { id } = useParams()` |

```jsx
navigate('/settings');             // 해당 주소로 이동
navigate(-1);                      // 뒤로 가기 (브라우저 ← 버튼)
navigate('/', { replace: true });  // 이동하되 방문 기록에 남기지 않음
```

- 클릭하면 **바로 이동**만 → `<Link>`
- 삭제·저장 같은 **작업을 한 뒤 이동** → `navigate()`

### 예시

**① useParams — 주소에서 할 일 id 꺼내기 (`src/pages/TodoDetailPage.jsx`)**

```jsx
import { Link, useNavigate, useParams } from 'react-router-dom';

function TodoDetailPage({ todos, onToggle, onDelete, onUpdate }) {
  const { id } = useParams(); // URL 파라미터는 항상 문자열
  const navigate = useNavigate();

  const todo = todos.find((t) => t.id === Number(id));

  // 없는 id로 접근한 경우 먼저 걸러낸다
  if (!todo) {
    return (
      <section className="detail">
        <p className="empty">해당 할 일을 찾을 수 없습니다. (id: {id})</p>
        <Link to="/">목록으로</Link>
      </section>
    );
  }
  // ...
}
```

- `/todos/1727236800000`로 들어오면 `id`는 `'1727236800000'` (**문자열**)
- 할 일의 `id`는 숫자(`Date.now()`)라서 `Number(id)`로 바꿔야 `find`가 찾는다
- 주소창에 아무 값이나 칠 수 있으므로 **못 찾은 경우를 먼저 처리**한다

**② useNavigate — 삭제 후 그날 목록으로 (`src/pages/TodoDetailPage.jsx`)**

```jsx
const handleDelete = () => {
  onDelete(todo.id);
  navigate(`/?date=${todo.date}`); // 삭제한 항목의 상세 페이지에 머무를 이유가 없으므로 그날 목록으로 이동
};

<button type="button" onClick={handleDelete}>
  삭제
</button>
```

**③ useNavigate — 뒤로 가기 (`src/pages/TodoDetailPage.jsx`)**

```jsx
<button type="button" onClick={() => navigate(-1)}>
  뒤로
</button>
```

**④ useNavigate — 전체 삭제 후 메인으로 (`src/pages/SettingsPage.jsx`)**

```jsx
const navigate = useNavigate();

const handleClearAll = () => {
  if (!window.confirm(`할 일 ${todos.length}개를 모두 삭제할까요?`)) return;
  onClearAll();
  navigate('/');
};
```

**⑤ 함께 쓴 것: useSearchParams — `?date=` 읽고 쓰기 (`src/pages/MainPage.jsx`)**

```jsx
import { useSearchParams } from 'react-router-dom';

const [searchParams, setSearchParams] = useSearchParams();

const selected = parseDateKey(searchParams.get('date'));   // '/?date=2026-09-25' → Date

const selectDate = (key) => setSearchParams({ date: key }, { replace: true });
```

| | `useParams` | `useSearchParams` |
|---|---|---|
| 주소 모양 | `/todos/123` | `/?date=2026-09-25` |
| Route 설정 | `path="todos/:id"` 필요 | 필요 없음 |
| 용도 | **어떤 것**을 볼지 (필수) | **어떻게** 볼지 (선택: 날짜, 필터 등) |

---

## 4. 중첩 라우트 개념

### 개념

`<Route>` 안에 `<Route>`를 넣어 **부모 화면 안에 자식 화면**을 그리는 것.
부모 컴포넌트는 자식이 들어갈 자리에 **`<Outlet />`** 을 둔다.

```jsx
<Route path="/" element={<Layout />}>      {/* 부모: 항상 보이는 틀 */}
  <Route index element={<Home />} />       {/* 자식: <Outlet/> 자리에 들어감 */}
  <Route path="about" element={<About />} />
</Route>
```

```jsx
function Layout() {
  return (
    <>
      <header>공통 헤더</header>
      <Outlet />          {/* 현재 주소에 맞는 자식 화면 */}
    </>
  );
}
```

- 자식 `path`는 **앞에 `/`를 붙이지 않는다** (`about` → 부모 경로에 이어 붙어 `/about`)
- 헤더·메뉴처럼 **여러 페이지가 공유하는 부분을 한 번만** 작성할 수 있다

### 예시

**모든 페이지가 공유하는 헤더 — `src/App.jsx` + `src/components/Layout.jsx`**

```jsx
// App.jsx
<Routes>
  <Route element={<Layout />}>                                  {/* 부모 (path 없음) */}
    <Route index element={<MainPage ... />} />                  {/* /          */}
    <Route path="todos/:id" element={<TodoDetailPage ... />} /> {/* /todos/:id */}
    <Route path="settings" element={<SettingsPage ... />} />    {/* /settings  */}
    <Route path="*" element={<Navigate to="/" replace />} />
  </Route>
</Routes>
```

```jsx
// Layout.jsx
import { NavLink, Outlet } from 'react-router-dom';

// 모든 페이지가 공유하는 헤더. <Outlet/> 자리에 현재 라우트의 페이지가 렌더링된다.
function Layout() {
  return (
    <div className="app">
      <header className="app-header">
        <h1>To-Do-List</h1>
        <nav className="app-nav">
          <NavLink to="/" end>
            달력
          </NavLink>
          <NavLink to="/settings">설정</NavLink>
        </nav>
      </header>
      <Outlet />
    </div>
  );
}
```

```
/settings 로 들어오면

<div class="app">
  <header> To-Do-List  [달력] [설정] </header>   ← Layout (항상)
  <SettingsPage />                               ← <Outlet/> 자리
</div>
```

부모 `Route`에 `path`가 없으므로 주소에는 영향을 주지 않고 **화면 틀만 제공**한다 (layout route).
헤더를 각 페이지마다 복사하지 않아도 되고, 페이지를 이동해도 헤더는 다시 만들어지지 않는다.

### 예시 (투두리스트에 없는 부분)

To-Do-List의 중첩은 **틀만 공유**하는 형태라, 주소가 **여러 단계로 이어지는** 중첩은 없어서 예시로 정리.

**할 일 상세 안에 탭 두기 — `/todos/:id`, `/todos/:id/memo`, `/todos/:id/edit`**

```jsx
<Routes>
  <Route element={<Layout />}>
    <Route index element={<MainPage />} />

    <Route path="todos/:id" element={<TodoLayout />}>   {/* /todos/:id 공통 틀 */}
      <Route index element={<TodoInfo />} />             {/* /todos/:id        */}
      <Route path="memo" element={<TodoMemo />} />       {/* /todos/:id/memo   */}
      <Route path="edit" element={<TodoEdit />} />       {/* /todos/:id/edit   */}
    </Route>
  </Route>
</Routes>
```

```jsx
import { NavLink, Outlet, useParams } from 'react-router-dom';

function TodoLayout() {
  const { id } = useParams();   // 자식 화면에서도 같은 id를 쓸 수 있다

  return (
    <section>
      <nav>
        <NavLink to={`/todos/${id}`} end>정보</NavLink>
        <NavLink to={`/todos/${id}/memo`}>세부사항</NavLink>
        <NavLink to={`/todos/${id}/edit`}>수정</NavLink>
      </nav>
      <Outlet />   {/* 정보 / 세부사항 / 수정 중 하나 */}
    </section>
  );
}
```

```
/todos/123/memo

Layout (헤더)
 └─ TodoLayout (정보 | 세부사항 | 수정 탭)
      └─ TodoMemo
```

부모 → 자식 → 손자로 `<Outlet />`이 이어지며, 주소도 `/todos/123` + `/memo`처럼 이어 붙는다.
지금 To-Do-List는 상세 페이지 안에서 `isEditing` state로 보기/수정을 바꾸는데, 이 방식으로 바꾸면 **수정 화면에도 고유한 주소**가 생겨 새로고침해도 수정 화면이 유지된다.

---

## 2주차 정리

| 개념 | 한 줄 요약 | 프로젝트 코드 |
|---|---|---|
| SPA 라우팅 | URL에 따라 보여줄 컴포넌트를 바꾼다 (새로고침 X) | `/`, `/todos/:id`, `/settings` |
| BrowserRouter | 앱 전체를 감싸 라우팅을 켠다 | `main.jsx` |
| Routes / Route | 주소 → 화면 규칙 | `<Route path="todos/:id" element={<TodoDetailPage />} />` |
| Link / NavLink | 새로고침 없는 이동 / 현재 메뉴 표시 | ``<Link to={`/todos/${todo.id}`}>`` |
| useNavigate | 작업 후 코드로 이동 | ``navigate(`/?date=${todo.date}`)``, `navigate(-1)` |
| useParams | 주소의 `:id` 꺼내기 (문자열) | `const { id } = useParams()` |
| 중첩 라우트 | 부모 틀 안에 자식 화면 (`<Outlet />`) | `<Route element={<Layout />}>` |
